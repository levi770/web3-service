export enum ProcessTypes {
  MINT = 'mint',
  DEPLOY = 'deploy',
}

export enum ObjectTypes {
  CONTRACT = 'contract',
  TOKEN = 'token',
}

export enum Networks {
  ETHEREUM = 5,
  POLYGON = 80001,
}

export enum FileTypes {
  IMAGE = 'image',
  OBJECT = 'object',
}
