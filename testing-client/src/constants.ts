export enum ProcessTypes {
  MINT = 'MINT',
  DEPLOY = 'DEPLOY',
}

export enum ObjectTypes {
  CONTRACT = 'CONTRACT',
  TOKEN = 'TOKEN',
}

export enum Networks {
  ETHEREUM = 1,
  POLYGON = 137,
}

export enum FileTypes {
  IMAGE = 'image',
  OBJECT = 'object',
}
